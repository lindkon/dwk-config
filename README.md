# dwk-config
## Important notes
1. Secrets are not added to the git repo so these need to manually applied.
2. The project uses a shared gateway with the exercises, so this configs does not add a gateway by itself